# Required for /api/inquiry SMTP delivery.
# Set real values in .env.local for local testing and in Vercel Production.
# Do not commit real SMTP passwords.
CONTACT_TO_EMAIL=exporter@justhen.co.jp
SMTP_HOST=mail1033.onamae.ne.jp
SMTP_PORT=465
SMTP_USER=exporter@justhen.co.jp
SMTP_PASS=
SMTP_FROM=exporter@justhen.co.jp
